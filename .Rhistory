)
# for (i in 1:nrow(pairs)) {
#   grsChr <- pairs$chr.grs[i]
#   grsStart <- pairs$start.grs[i]
#   grsEnd <- pairs$end.grs[i]
#   probeChr <- pairs$chr.probe[i]
#   probeStart <- pairs$start.probe[i]
#   probeEnd <- pairs$end.probe[i]
#   circos.link(grsChr, c(grsStart, grsEnd), probeChr, c(probeStart, probeEnd), col = "#ecc771")
# }
for (currentGRS in sample( unique(pairs$grs), 5 )) {
probes <- pairs$probe[pairs$grs == currentGRS]
grsStart <- grsSectors$grs_sector_start[grsSectors$grs == currentGRS]
grsEnd <- grsSectors$grs_sector_end[grsSectors$grs == currentGRS]
grsChr <- grsSectors$chr[grsSectors$grs == currentGRS] %>% as.character() %>% as.numeric()
# circos.par(points.overflow.warning = FALSE)
# factors <- 1:22
# circos.initialize(factors = factors, xlim = xlim)
# circos.track(
#   factors = factors,
#   ylim = c(0, 1), bg.col = "grey",
#   bg.border = NA, track.height = 0.05
# )
for (probe in probes) {
probeStart <- probeSectors$probe_sector_start[probeSectors$probe == probe]
probeEnd <- probeSectors$probe_sector_end[probeSectors$probe == probe]
probeChr <- probeSectors$chr[probeSectors$probe == probe] %>% as.character() %>% as.numeric()
circos.link(grsChr, c(grsStart, grsEnd), probeChr, c(probeStart, probeEnd), col = "#ecc771")
}
title(main = list("5", col = "darkgrey"))
}
circos.par(points.overflow.warning = FALSE)
factors <- 1:22
circos.initialize(factors = factors, xlim = xlim)
circos.track(
factors = factors,
ylim = c(0, 1), bg.col = "grey",
bg.border = NA, track.height = 0.05
)
# for (i in 1:nrow(pairs)) {
#   grsChr <- pairs$chr.grs[i]
#   grsStart <- pairs$start.grs[i]
#   grsEnd <- pairs$end.grs[i]
#   probeChr <- pairs$chr.probe[i]
#   probeStart <- pairs$start.probe[i]
#   probeEnd <- pairs$end.probe[i]
#   circos.link(grsChr, c(grsStart, grsEnd), probeChr, c(probeStart, probeEnd), col = "#ecc771")
# }
for (currentGRS in sample( unique(pairs$grs), 10 )) {
probes <- pairs$probe[pairs$grs == currentGRS]
grsStart <- grsSectors$grs_sector_start[grsSectors$grs == currentGRS]
grsEnd <- grsSectors$grs_sector_end[grsSectors$grs == currentGRS]
grsChr <- grsSectors$chr[grsSectors$grs == currentGRS] %>% as.character() %>% as.numeric()
# circos.par(points.overflow.warning = FALSE)
# factors <- 1:22
# circos.initialize(factors = factors, xlim = xlim)
# circos.track(
#   factors = factors,
#   ylim = c(0, 1), bg.col = "grey",
#   bg.border = NA, track.height = 0.05
# )
for (probe in probes) {
probeStart <- probeSectors$probe_sector_start[probeSectors$probe == probe]
probeEnd <- probeSectors$probe_sector_end[probeSectors$probe == probe]
probeChr <- probeSectors$chr[probeSectors$probe == probe] %>% as.character() %>% as.numeric()
circos.link(grsChr, c(grsStart, grsEnd), probeChr, c(probeStart, probeEnd), col = "#ecc771")
}
title(main = list("5", col = "darkgrey"))
}
setEPS()
postscript("circos-test.eps", height = 6, width = 6)
circos.par(points.overflow.warning = FALSE)
factors <- 1:22
circos.initialize(factors = factors, xlim = xlim)
circos.track(
factors = factors,
ylim = c(0, 1), bg.col = "grey",
bg.border = NA, track.height = 0.05
)
# for (i in 1:nrow(pairs)) {
#   grsChr <- pairs$chr.grs[i]
#   grsStart <- pairs$start.grs[i]
#   grsEnd <- pairs$end.grs[i]
#   probeChr <- pairs$chr.probe[i]
#   probeStart <- pairs$start.probe[i]
#   probeEnd <- pairs$end.probe[i]
#   circos.link(grsChr, c(grsStart, grsEnd), probeChr, c(probeStart, probeEnd), col = "#ecc771")
# }
for (currentGRS in sample( unique(pairs$grs), 10 )) {
probes <- pairs$probe[pairs$grs == currentGRS]
grsStart <- grsSectors$grs_sector_start[grsSectors$grs == currentGRS]
grsEnd <- grsSectors$grs_sector_end[grsSectors$grs == currentGRS]
grsChr <- grsSectors$chr[grsSectors$grs == currentGRS] %>% as.character() %>% as.numeric()
# circos.par(points.overflow.warning = FALSE)
# factors <- 1:22
# circos.initialize(factors = factors, xlim = xlim)
# circos.track(
#   factors = factors,
#   ylim = c(0, 1), bg.col = "grey",
#   bg.border = NA, track.height = 0.05
# )
for (probe in probes) {
probeStart <- probeSectors$probe_sector_start[probeSectors$probe == probe]
probeEnd <- probeSectors$probe_sector_end[probeSectors$probe == probe]
probeChr <- probeSectors$chr[probeSectors$probe == probe] %>% as.character() %>% as.numeric()
circos.link(grsChr, c(grsStart, grsEnd), probeChr, c(probeStart, probeEnd), col = "#ecc771")
}
title(main = list("5", col = "darkgrey"))
}
dev.off()
setEPS()
postscript("circos-test.eps", height = 6, width = 6)
circos.par(points.overflow.warning = FALSE)
factors <- 1:22
circos.initialize(factors = factors, xlim = xlim)
circos.track(
factors = factors,
ylim = c(0, 1), bg.col = "grey",
bg.border = NA, track.height = 0.05
)
# for (i in 1:nrow(pairs)) {
#   grsChr <- pairs$chr.grs[i]
#   grsStart <- pairs$start.grs[i]
#   grsEnd <- pairs$end.grs[i]
#   probeChr <- pairs$chr.probe[i]
#   probeStart <- pairs$start.probe[i]
#   probeEnd <- pairs$end.probe[i]
#   circos.link(grsChr, c(grsStart, grsEnd), probeChr, c(probeStart, probeEnd), col = "#ecc771")
# }
for (currentGRS in sample( unique(pairs$grs), 10 )) {
probes <- pairs$probe[pairs$grs == currentGRS]
grsStart <- grsSectors$grs_sector_start[grsSectors$grs == currentGRS]
grsEnd <- grsSectors$grs_sector_end[grsSectors$grs == currentGRS]
grsChr <- grsSectors$chr[grsSectors$grs == currentGRS] %>% as.character() %>% as.numeric()
# circos.par(points.overflow.warning = FALSE)
# factors <- 1:22
# circos.initialize(factors = factors, xlim = xlim)
# circos.track(
#   factors = factors,
#   ylim = c(0, 1), bg.col = "grey",
#   bg.border = NA, track.height = 0.05
# )
for (probe in probes) {
probeStart <- probeSectors$probe_sector_start[probeSectors$probe == probe]
probeEnd <- probeSectors$probe_sector_end[probeSectors$probe == probe]
probeChr <- probeSectors$chr[probeSectors$probe == probe] %>% as.character() %>% as.numeric()
circos.link(grsChr, c(grsStart, grsEnd), probeChr, c(probeStart, probeEnd), col = "#ecc771")
}
}
dev.off()
pal <- brewer.pal(n = 5, name = "Dark2")
pal <- brewer.pal(n = 5, name = "Dark2")
pal
brewer.pal(n = 10, name = "Dark2")
brewer.pal(n = 8, name = "Dark2")
tab <- sort(table(pairs$grs), decreasing = TRUE)
pal <- brewer.pal(n = 8, name = "Dark2")
names(pal) <- pal
for (currentGRS in head(names(tab), 8)) {
probes <- pairs$probe[pairs$grs == currentGRS]
grsStart <- grsSectors$grs_sector_start[grsSectors$grs == currentGRS]
grsEnd <- grsSectors$grs_sector_end[grsSectors$grs == currentGRS]
grsChr <- grsSectors$chr[grsSectors$grs == currentGRS] %>% as.character() %>% as.numeric()
# circos.par(points.overflow.warning = FALSE)
# factors <- 1:22
# circos.initialize(factors = factors, xlim = xlim)
# circos.track(
#   factors = factors,
#   ylim = c(0, 1), bg.col = "grey",
#   bg.border = NA, track.height = 0.05
# )
for (probe in probes) {
probeStart <- probeSectors$probe_sector_start[probeSectors$probe == probe]
probeEnd <- probeSectors$probe_sector_end[probeSectors$probe == probe]
probeChr <- probeSectors$chr[probeSectors$probe == probe] %>% as.character() %>% as.numeric()
circos.link(grsChr, c(grsStart, grsEnd), probeChr, c(probeStart, probeEnd), col = pal[currentGRS])
}
}
circos.par(points.overflow.warning = FALSE)
factors <- 1:22
circos.initialize(factors = factors, xlim = xlim)
circos.track(
factors = factors,
ylim = c(0, 1), bg.col = "grey",
bg.border = NA, track.height = 0.05
)
# for (i in 1:nrow(pairs)) {
#   grsChr <- pairs$chr.grs[i]
#   grsStart <- pairs$start.grs[i]
#   grsEnd <- pairs$end.grs[i]
#   probeChr <- pairs$chr.probe[i]
#   probeStart <- pairs$start.probe[i]
#   probeEnd <- pairs$end.probe[i]
#   circos.link(grsChr, c(grsStart, grsEnd), probeChr, c(probeStart, probeEnd), col = "#ecc771")
# }
tab <- sort(table(pairs$grs), decreasing = TRUE)
pal <- brewer.pal(n = 8, name = "Dark2")
names(pal) <- pal
for (currentGRS in head(names(tab), 8)) {
probes <- pairs$probe[pairs$grs == currentGRS]
grsStart <- grsSectors$grs_sector_start[grsSectors$grs == currentGRS]
grsEnd <- grsSectors$grs_sector_end[grsSectors$grs == currentGRS]
grsChr <- grsSectors$chr[grsSectors$grs == currentGRS] %>% as.character() %>% as.numeric()
# circos.par(points.overflow.warning = FALSE)
# factors <- 1:22
# circos.initialize(factors = factors, xlim = xlim)
# circos.track(
#   factors = factors,
#   ylim = c(0, 1), bg.col = "grey",
#   bg.border = NA, track.height = 0.05
# )
for (probe in probes) {
probeStart <- probeSectors$probe_sector_start[probeSectors$probe == probe]
probeEnd <- probeSectors$probe_sector_end[probeSectors$probe == probe]
probeChr <- probeSectors$chr[probeSectors$probe == probe] %>% as.character() %>% as.numeric()
circos.link(grsChr, c(grsStart, grsEnd), probeChr, c(probeStart, probeEnd), col = pal[currentGRS])
}
}
pal[currentGRS]
pal
names(pal) <- names(tab)
tab <- sort(table(pairs$grs), decreasing = TRUE)
pal <- brewer.pal(n = 8, name = "Dark2")
names(pal) <- head(names(tab), 8)
for (currentGRS in names(pal)) {
probes <- pairs$probe[pairs$grs == currentGRS]
grsStart <- grsSectors$grs_sector_start[grsSectors$grs == currentGRS]
grsEnd <- grsSectors$grs_sector_end[grsSectors$grs == currentGRS]
grsChr <- grsSectors$chr[grsSectors$grs == currentGRS] %>% as.character() %>% as.numeric()
# circos.par(points.overflow.warning = FALSE)
# factors <- 1:22
# circos.initialize(factors = factors, xlim = xlim)
# circos.track(
#   factors = factors,
#   ylim = c(0, 1), bg.col = "grey",
#   bg.border = NA, track.height = 0.05
# )
for (probe in probes) {
probeStart <- probeSectors$probe_sector_start[probeSectors$probe == probe]
probeEnd <- probeSectors$probe_sector_end[probeSectors$probe == probe]
probeChr <- probeSectors$chr[probeSectors$probe == probe] %>% as.character() %>% as.numeric()
circos.link(grsChr, c(grsStart, grsEnd), probeChr, c(probeStart, probeEnd), col = pal[currentGRS])
}
}
require(RColorBrewer)
library(circlize)
setEPS()
postscript("circos-test.eps", height = 6, width = 6)
circos.par(points.overflow.warning = FALSE)
factors <- 1:22
circos.initialize(factors = factors, xlim = xlim)
circos.track(
factors = factors,
ylim = c(0, 1), bg.col = "grey",
bg.border = NA, track.height = 0.05
)
# for (i in 1:nrow(pairs)) {
#   grsChr <- pairs$chr.grs[i]
#   grsStart <- pairs$start.grs[i]
#   grsEnd <- pairs$end.grs[i]
#   probeChr <- pairs$chr.probe[i]
#   probeStart <- pairs$start.probe[i]
#   probeEnd <- pairs$end.probe[i]
#   circos.link(grsChr, c(grsStart, grsEnd), probeChr, c(probeStart, probeEnd), col = "#ecc771")
# }
tab <- sort(table(pairs$grs), decreasing = TRUE)
pal <- brewer.pal(n = 8, name = "Dark2")
names(pal) <- head(names(tab), 8)
for (currentGRS in names(pal)) {
probes <- pairs$probe[pairs$grs == currentGRS]
grsStart <- grsSectors$grs_sector_start[grsSectors$grs == currentGRS]
grsEnd <- grsSectors$grs_sector_end[grsSectors$grs == currentGRS]
grsChr <- grsSectors$chr[grsSectors$grs == currentGRS] %>% as.character() %>% as.numeric()
# circos.par(points.overflow.warning = FALSE)
# factors <- 1:22
# circos.initialize(factors = factors, xlim = xlim)
# circos.track(
#   factors = factors,
#   ylim = c(0, 1), bg.col = "grey",
#   bg.border = NA, track.height = 0.05
# )
for (probe in probes) {
probeStart <- probeSectors$probe_sector_start[probeSectors$probe == probe]
probeEnd <- probeSectors$probe_sector_end[probeSectors$probe == probe]
probeChr <- probeSectors$chr[probeSectors$probe == probe] %>% as.character() %>% as.numeric()
circos.link(grsChr, c(grsStart, grsEnd), probeChr, c(probeStart, probeEnd), col = pal[currentGRS])
}
}
dev.off()
brewer.pal(n = 8, name = "Dark2")
brewer.pal(n = 8, name = "RdYlGn")
circos.par(points.overflow.warning = FALSE)
factors <- 1:22
circos.initialize(factors = factors, xlim = xlim)
circos.track(
factors = factors,
ylim = c(0, 1), bg.col = "grey",
bg.border = NA, track.height = 0.05
)
# for (i in 1:nrow(pairs)) {
#   grsChr <- pairs$chr.grs[i]
#   grsStart <- pairs$start.grs[i]
#   grsEnd <- pairs$end.grs[i]
#   probeChr <- pairs$chr.probe[i]
#   probeStart <- pairs$start.probe[i]
#   probeEnd <- pairs$end.probe[i]
#   circos.link(grsChr, c(grsStart, grsEnd), probeChr, c(probeStart, probeEnd), col = "#ecc771")
# }
tab <- sort(table(pairs$grs), decreasing = TRUE)
pal <- brewer.pal(n = 8, name = "RdYlGn")
names(pal) <- head(names(tab), 8)
for (currentGRS in names(pal)) {
probes <- pairs$probe[pairs$grs == currentGRS]
grsStart <- grsSectors$grs_sector_start[grsSectors$grs == currentGRS]
grsEnd <- grsSectors$grs_sector_end[grsSectors$grs == currentGRS]
grsChr <- grsSectors$chr[grsSectors$grs == currentGRS] %>% as.character() %>% as.numeric()
# circos.par(points.overflow.warning = FALSE)
# factors <- 1:22
# circos.initialize(factors = factors, xlim = xlim)
# circos.track(
#   factors = factors,
#   ylim = c(0, 1), bg.col = "grey",
#   bg.border = NA, track.height = 0.05
# )
for (probe in probes) {
probeStart <- probeSectors$probe_sector_start[probeSectors$probe == probe]
probeEnd <- probeSectors$probe_sector_end[probeSectors$probe == probe]
probeChr <- probeSectors$chr[probeSectors$probe == probe] %>% as.character() %>% as.numeric()
circos.link(grsChr, c(grsStart, grsEnd), probeChr, c(probeStart, probeEnd), col = pal[currentGRS])
}
}
require(RColorBrewer)
library(circlize)
# setEPS()
# postscript("circos-test.eps", height = 6, width = 6)
circos.par(points.overflow.warning = FALSE)
factors <- 1:22
circos.initialize(factors = factors, xlim = xlim)
circos.track(
factors = factors,
ylim = c(0, 1), bg.col = "grey",
bg.border = NA, track.height = 0.05
)
# for (i in 1:nrow(pairs)) {
#   grsChr <- pairs$chr.grs[i]
#   grsStart <- pairs$start.grs[i]
#   grsEnd <- pairs$end.grs[i]
#   probeChr <- pairs$chr.probe[i]
#   probeStart <- pairs$start.probe[i]
#   probeEnd <- pairs$end.probe[i]
#   circos.link(grsChr, c(grsStart, grsEnd), probeChr, c(probeStart, probeEnd), col = "#ecc771")
# }
tab <- sort(table(pairs$grs), decreasing = TRUE)
pal <- brewer.pal(n = 8, name = "YlOrRd")
names(pal) <- head(names(tab), 8)
for (currentGRS in names(pal)) {
probes <- pairs$probe[pairs$grs == currentGRS]
grsStart <- grsSectors$grs_sector_start[grsSectors$grs == currentGRS]
grsEnd <- grsSectors$grs_sector_end[grsSectors$grs == currentGRS]
grsChr <- grsSectors$chr[grsSectors$grs == currentGRS] %>% as.character() %>% as.numeric()
# circos.par(points.overflow.warning = FALSE)
# factors <- 1:22
# circos.initialize(factors = factors, xlim = xlim)
# circos.track(
#   factors = factors,
#   ylim = c(0, 1), bg.col = "grey",
#   bg.border = NA, track.height = 0.05
# )
for (probe in probes) {
probeStart <- probeSectors$probe_sector_start[probeSectors$probe == probe]
probeEnd <- probeSectors$probe_sector_end[probeSectors$probe == probe]
probeChr <- probeSectors$chr[probeSectors$probe == probe] %>% as.character() %>% as.numeric()
circos.link(grsChr, c(grsStart, grsEnd), probeChr, c(probeStart, probeEnd), col = pal[currentGRS])
}
}
# dev.off()
require(RColorBrewer)
library(circlize)
# setEPS()
# postscript("circos-test.eps", height = 6, width = 6)
circos.par(points.overflow.warning = FALSE)
factors <- 1:22
circos.initialize(factors = factors, xlim = xlim)
circos.track(
factors = factors,
ylim = c(0, 1), bg.col = "grey",
bg.border = NA, track.height = 0.05
)
# for (i in 1:nrow(pairs)) {
#   grsChr <- pairs$chr.grs[i]
#   grsStart <- pairs$start.grs[i]
#   grsEnd <- pairs$end.grs[i]
#   probeChr <- pairs$chr.probe[i]
#   probeStart <- pairs$start.probe[i]
#   probeEnd <- pairs$end.probe[i]
#   circos.link(grsChr, c(grsStart, grsEnd), probeChr, c(probeStart, probeEnd), col = "#ecc771")
# }
tab <- sort(table(pairs$grs), decreasing = TRUE)
pal <- brewer.pal(n = 8, name = "OrRd")
names(pal) <- head(names(tab), 8)
for (currentGRS in names(pal)) {
probes <- pairs$probe[pairs$grs == currentGRS]
grsStart <- grsSectors$grs_sector_start[grsSectors$grs == currentGRS]
grsEnd <- grsSectors$grs_sector_end[grsSectors$grs == currentGRS]
grsChr <- grsSectors$chr[grsSectors$grs == currentGRS] %>% as.character() %>% as.numeric()
# circos.par(points.overflow.warning = FALSE)
# factors <- 1:22
# circos.initialize(factors = factors, xlim = xlim)
# circos.track(
#   factors = factors,
#   ylim = c(0, 1), bg.col = "grey",
#   bg.border = NA, track.height = 0.05
# )
for (probe in probes) {
probeStart <- probeSectors$probe_sector_start[probeSectors$probe == probe]
probeEnd <- probeSectors$probe_sector_end[probeSectors$probe == probe]
probeChr <- probeSectors$chr[probeSectors$probe == probe] %>% as.character() %>% as.numeric()
circos.link(grsChr, c(grsStart, grsEnd), probeChr, c(probeStart, probeEnd), col = pal[currentGRS])
}
}
# dev.off()
require(RColorBrewer)
library(circlize)
setEPS()
postscript("circos-test.eps", height = 6, width = 6)
circos.par(points.overflow.warning = FALSE)
factors <- 1:22
circos.initialize(factors = factors, xlim = xlim)
circos.track(
factors = factors,
ylim = c(0, 1), bg.col = "grey",
bg.border = NA, track.height = 0.05
)
# for (i in 1:nrow(pairs)) {
#   grsChr <- pairs$chr.grs[i]
#   grsStart <- pairs$start.grs[i]
#   grsEnd <- pairs$end.grs[i]
#   probeChr <- pairs$chr.probe[i]
#   probeStart <- pairs$start.probe[i]
#   probeEnd <- pairs$end.probe[i]
#   circos.link(grsChr, c(grsStart, grsEnd), probeChr, c(probeStart, probeEnd), col = "#ecc771")
# }
tab <- sort(table(pairs$grs), decreasing = TRUE)
pal <- brewer.pal(n = 8, name = "OrRd")
names(pal) <- head(names(tab), 8)
for (currentGRS in names(pal)) {
probes <- pairs$probe[pairs$grs == currentGRS]
grsStart <- grsSectors$grs_sector_start[grsSectors$grs == currentGRS]
grsEnd <- grsSectors$grs_sector_end[grsSectors$grs == currentGRS]
grsChr <- grsSectors$chr[grsSectors$grs == currentGRS] %>% as.character() %>% as.numeric()
# circos.par(points.overflow.warning = FALSE)
# factors <- 1:22
# circos.initialize(factors = factors, xlim = xlim)
# circos.track(
#   factors = factors,
#   ylim = c(0, 1), bg.col = "grey",
#   bg.border = NA, track.height = 0.05
# )
for (probe in probes) {
probeStart <- probeSectors$probe_sector_start[probeSectors$probe == probe]
probeEnd <- probeSectors$probe_sector_end[probeSectors$probe == probe]
probeChr <- probeSectors$chr[probeSectors$probe == probe] %>% as.character() %>% as.numeric()
circos.link(grsChr, c(grsStart, grsEnd), probeChr, c(probeStart, probeEnd), col = pal[currentGRS])
}
}
dev.off()
require(RColorBrewer)
library(circlize)
setEPS()
postscript("circos-test.eps", height = 6, width = 6)
circos.par(points.overflow.warning = FALSE)
factors <- 1:22
circos.initialize(factors = factors, xlim = xlim)
circos.track(
factors = factors,
ylim = c(0, 1), bg.col = "grey",
bg.border = NA, track.height = 0.05
)
# for (i in 1:nrow(pairs)) {
#   grsChr <- pairs$chr.grs[i]
#   grsStart <- pairs$start.grs[i]
#   grsEnd <- pairs$end.grs[i]
#   probeChr <- pairs$chr.probe[i]
#   probeStart <- pairs$start.probe[i]
#   probeEnd <- pairs$end.probe[i]
#   circos.link(grsChr, c(grsStart, grsEnd), probeChr, c(probeStart, probeEnd), col = "#ecc771")
# }
tab <- sort(table(pairs$grs), decreasing = TRUE)
pal <- brewer.pal(n = 8, name = "OrRd")
pal <- c("#c57b3d", "#c2c991", "#b2dae7", "#c1cb4d", "#bc7244", "#d4c650", "#b6994e")
names(pal) <- head(names(tab), length(pal))
for (currentGRS in names(pal)) {
probes <- pairs$probe[pairs$grs == currentGRS]
grsStart <- grsSectors$grs_sector_start[grsSectors$grs == currentGRS]
grsEnd <- grsSectors$grs_sector_end[grsSectors$grs == currentGRS]
grsChr <- grsSectors$chr[grsSectors$grs == currentGRS] %>% as.character() %>% as.numeric()
# circos.par(points.overflow.warning = FALSE)
# factors <- 1:22
# circos.initialize(factors = factors, xlim = xlim)
# circos.track(
#   factors = factors,
#   ylim = c(0, 1), bg.col = "grey",
#   bg.border = NA, track.height = 0.05
# )
for (probe in probes) {
probeStart <- probeSectors$probe_sector_start[probeSectors$probe == probe]
probeEnd <- probeSectors$probe_sector_end[probeSectors$probe == probe]
probeChr <- probeSectors$chr[probeSectors$probe == probe] %>% as.character() %>% as.numeric()
circos.link(grsChr, c(grsStart, grsEnd), probeChr, c(probeStart, probeEnd), col = pal[currentGRS])
}
}
dev.off()
